server:
  port: 8080

spring:
  application:
    name: metadata
  datasource:
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=monday_metadata